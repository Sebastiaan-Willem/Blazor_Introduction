@page "/"
@page "/home"
@inject IMovieService service

@*load component in razor page*@
<MovieList Movies="@movies" />

@code{

    List<Movie> movies;

    protected override void OnInitialized()
    {
        //DEPENDENCY INJECTION IN BLAZOR -> top of the page
        movies = service.GetMovies();
    }
}